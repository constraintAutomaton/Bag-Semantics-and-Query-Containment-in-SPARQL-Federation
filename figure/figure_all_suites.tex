\begin{figure}[t]
  \centering
  \includegraphics[width=\linewidth]{figure/all_suites.pdf}
  \caption{Distribution of the execution times of \solverName{} and SpeCS over the pairs each answers correctly, with the median marked.
\solverName{} reaches far lower times, and its tail extends further but remains thin.}
  \label{fig:all_suites_bfc_specs}
\end{figure}
