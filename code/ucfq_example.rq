PREFIX ex: <http://example.org/>
SELECT * WHERE {
  { SERVICE ex:jobRegistry { ?s ex:job ?job } }
  UNION
  { SERVICE ex:socialNetwork { ?s ex:job ?job } }

  { SERVICE ex:jobRegistry { ?s ex:age ?age } }
  UNION
  { SERVICE ex:socialNetwork { ?s ex:age ?age } }
}
