\section{Related Work}\label{sec:related_works}

\subsection{Query Containment}\label{sec:related_works_qc}

Query containment is the problem of deciding, for every possible database, whether the result of a query $Q_1$ is a subset of the result of a query $Q_2$, denoted $Q_1 \sqsubseteq Q_2$; in this case $Q_1$ is the \emph{contained query} and $Q_2$ the \emph{containing query}.
Query containment has been used in multiple query optimization tasks, including view selection, caching, and query rewriting~\cite{Afrati2019-ch2, Shirkova2011, Halevy2001}.

Query containment under bag or bag-set semantics is a strictly stronger relationship than its set-semantic counterpart~\cite{Chaudhuri1993}.
Containment of \acp{UCQ} is undecidable under bag semantics~\cite{ioannidis1995containment}, while under bag-set semantics it remains, to our knowledge, an open problem.
For \acp{CQ}, no general necessary and sufficient condition for containment under bag-set and bag semantics is known~\cite{ioannidis1995containment, khamis2021bag}; however, sufficient conditions have been identified, such as the existence of an onto containment mapping under bag semantics, or of a variable-onto one under bag-set semantics, both NP-complete to decide~\cite{Chaudhuri1993}.
Containment of \acp{CQ} is $\Pi^p_2$-hard and its decidability remains open~\cite{Chaudhuri1993, khamis2021bag}, while some fragments have been proven decidable~\cite{khamis2021bag, KOPPARTY20111097, Chaudhuri1993, Afrati2010}.
\Cref{tab_decidable_containment_bag_bag_set} summarizes the known results on bag and bag-set \ac{CQ} containment relevant to this paper.

\begin{table*}
  \caption{Known results on bag and bag-set \ac{CQ} containment for the fragments discussed in this paper.
The general case remains open, so deciding containment relies on sufficient conditions and on the fragments with exact results.}
  \label{tab_decidable_containment_bag_bag_set}
  \begin{tabular}{llll}
    \toprule
    Case & Semantic & Result & Complexity\\
    \midrule
    General \ac{UCQ}~\cite{ioannidis1995containment} & Bag & Exact & Undecidable\\
    General \ac{UCQ} & Bag-set & Open & Open\\
    General \ac{CQ}~\cite{Chaudhuri1993} & Bag, bag-set & Lower bound, decidability open & $\Pi^p_2$-hard\\
    Onto containment mapping~\cite{Chaudhuri1993} & Bag & Sufficient, not necessary & NP-complete\\
    Variable-onto containment mapping~\cite{Chaudhuri1993} & Bag-set & Sufficient, not necessary & NP-complete\\
    \ac{CQ}, $Q_1$ projection-free~\cite{Afrati2010} & Bag-set & Exact & NP-complete\\
    \ac{CQ}, $Q_1$ projection-free~\cite{Afrati2010} & Bag & Open & Open\\
    \ac{CQ}, $Q_1,Q_2$ projection-free~\cite{Afrati2010} & Bag-set & Exact & $O(n^2\log n)$\\
    \ac{CQ}, $Q_1,Q_2$ projection-free~\cite{Afrati2010} & Bag & Exact & $O(n^2\log n)$\\
    \ac{CQ}, $Q_1$ without self-joins~\cite{ioannidis1995containment, Afrati2010} & Bag, bag-set & Exact & $O(n\log n)$\\
    \bottomrule
  \end{tabular}
\end{table*}

Beyond relational databases, query containment has been studied in other graph query languages, including XPath~\cite{schwentick2004xpath} and Cypher~\cite{tang2025proving}.
In the context of SPARQL, set-semantic query containment has received considerable attention~\cite{WudageChekol2013, letelier2013static, chekol2012sparql, chekol2012sparql2, Stadler2018, jenaQC, Spasic2023}.
Extensions incorporating RDF schemas, OWL entailment regimes~\cite{WudageChekol2013}, and property paths~\cite{Kostylev2025, chekol2016containment} have also been proposed, though these fall outside the scope of this paper.


\subsection{Federated Queries}\label{sec:related_works_fed}

%% We need to clarify why we take bag semantic FEDQPL
Federated SPARQL queries have been approached from two perspectives: explicit federation through the \texttt{SERVICE} clause, and automatic source selection.
The SPARQL 1.1 Federated Query specification~\cite{w3SPARQLFederated} introduces the \texttt{SERVICE} clause as the W3C standard mechanism for directing subexpressions to specific endpoints.
The SPARQL specification mentions the use of variables as endpoint URLs in \texttt{SERVICE} clauses but does not give an official interpretation of their behavior.\footnote{\url{https://www.w3.org/TR/sparql11-federated-query/\#variableService}}
\citeauthor{BuilAranda2013}~\cite{BuilAranda2013} introduced \emph{Service-Safeness}, a syntactic rule outside the SPARQL specification that makes such queries computable by guaranteeing a finite set of federation members.
Because Service-Safeness is not part of the specification and multiple SPARQL query engines use their own interpretation, this paper does not consider variables as endpoint URLs.
\citeauthor{Cheng2021}~\cite{Cheng2021} proposed FedQPL, a formalism to express federated queries under automatic source selection.
Automatic source selection algorithms~\cite{schwarte2011fedx, gorlitz2011splendid, acosta2011anapsid, charalambidis2015semagrow, saleem2018costfed, taelman_iswc_resources_comunica_2018} alleviate the practical difficulty of determining which federation member each subquery should be sent to~\cite{hanski2025observations}.
Thus, users can query a federation without knowing how the data is distributed.
Furthermore, \citeauthor{Cheng2021}~\cite{Cheng2021} introduced \ac{ESA}, a canonical source-selection algorithm that requires no prior information about the federation.
In \ac{ESA}, each triple pattern is evaluated against every federation member and the results are combined via union, guaranteeing complete results regardless of how data is distributed across the federation.

To the best of our knowledge, no contribution has addressed query containment for federated SPARQL queries; this is the gap our paper addresses.
