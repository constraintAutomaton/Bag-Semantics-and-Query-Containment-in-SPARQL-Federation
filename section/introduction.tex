\section{Introduction}

Minimizing redundant computation is a fundamental concern in query optimization.
Query containment is a classic tool for this purpose: it determines whether the results of one query are always included in those of another, independently of the targeted database.
Solving this problem underpins a wide range of optimization techniques, including caching, view selection, query rewriting, and equivalence validation~\cite{Afrati2019-ch1, DOAN201221, Chaudhuri1993}.
Under set semantics, multiple query fragments have been thoroughly characterized~\cite{Chandra1977, aho1979equivalences, aho1979efficient, Chekuri2000}.
Bag-semantic and bag-set semantic containment have also been studied~\cite{Chaudhuri1993, Afrati2010, khamis2021bag}; however, several query fragments remain undecidable under these semantics and open problems persist.

\ac{KG} has become a widely adopted paradigm for representing and integrating heterogeneous data, enabling applications in scientific data management, enterprise knowledge representation, and the Web of Data.
SPARQL is the standard query language for \ac{KG} built on \ac{RDF}, and federated SPARQL extends this by enabling virtual data integration across distributed \ac{KG} sources, allowing heterogeneous data to be queried jointly without centralization, which is particularly valuable when sources are governed by different policies, maintained by independent parties, or too large to consolidate.
However, federated queries impose significant computational demands on both endpoints and clients, a challenge further compounded by the reliability limitations of public SPARQL endpoints, which frequently experience downtime or performance degradation under load~\cite{buil2013sparql}.
In settings such as scientific reproducibility studies, where queries must be re-executed or iteratively refined, this overhead is especially costly.
Query containment is therefore a natural optimization: when a new query is contained in a previously executed one, results can be derived from cached data without re-querying the federation, reducing both endpoint load and network traffic.

For SPARQL, existing research has addressed set-semantic query containment~\cite{WudageChekol2013}.
Yet the SPARQL query language operates under bag-set semantics~\cite{Perez2006, Angles2016}, in which the underlying database operates under set semantics~\cite{w3ConceptsAbstract} while query operators produce bags\footnote{A bag, or multiset, is similar to set but allows duplicate elements.}~\cite{w3SPARQLQuery}.
Set-semantic containment does not imply bag-set semantic containment, making existing containment results invalid for real-world SPARQL query optimization.

For example\footnote{Inspired by \citeauthor{Afrati2010}}, \Cref{fig:bag-set-example}  presents a super query (a query having producing more results independent) and the sub query.
In a set semantic context both queries would be contained, however in a bag-set semantic context due to the project and if the data contain individual with multiple jobs,
the query result will not be the same, thus using a set-semantic solver would err in the computation.\footnote{An executable example is provided at \url{https://github.com/constraintAutomaton/demo-semantic-query-containment-SPARQL} using the Comunica~\cite{taelman_iswc_resources_comunica_2018} query engine and the Specs~\cite{Spasic2023} SPARQL query containment solver}

\begin{figure}
  \textbf{Super query}
  \lstinputlisting[style=sparql]{code/bag_set_super_q.rq}
  \textbf{Sub query}
  \lstinputlisting[style=sparql]{code/bag_set_sub_q.rq}
  \caption{A super query and a sub query that are contained under set semantics but not under bag-set semantics.}
  \label{fig:bag-set-example}
\end{figure}

The problem is further compounded by the fact that there are two formalisms of federated queries: the SPARQL 1.1 Federated Query specification~\cite{w3SPARQLFederated} and \ac{ESA}~\cite{Cheng2021}.
For \ac{CQ}, those two formalisms do not exhibit the same semantics.

\begin{figure*}
  \begin{minipage}{0.20\textwidth}
    \textbf{Identifier registry}\\
    \texttt{\scriptsize ex:identifierRegistry}
    \lstinputlisting[basicstyle=\ttfamily\scriptsize,breaklines=true,firstline=3]{code/bag_federation_id.ttl}
  \end{minipage}\hfill
  \begin{minipage}{0.36\textwidth}
    \textbf{Job registry}\\
    \texttt{\scriptsize ex:jobRegistry}
    \lstinputlisting[basicstyle=\ttfamily\scriptsize,breaklines=true,firstline=3]{code/bag_federation_job.ttl}
  \end{minipage}\hfill
  \begin{minipage}{0.42\textwidth}
    \textbf{Social network}\\
    \texttt{\scriptsize ex:socialNetwork}
    \lstinputlisting[basicstyle=\ttfamily\scriptsize,breaklines=true,firstline=3]{code/bag_federation_social.ttl}
  \end{minipage}

  \medskip

  \begin{minipage}{0.48\textwidth}
    \textbf{Federated query under ESA}
    \lstinputlisting[style=sparql]{code/bag_federation_esa_q.rq}
  \end{minipage}\hfill
  \begin{minipage}{0.48\textwidth}
    \textbf{The same query with \texttt{SERVICE} clauses}
    \lstinputlisting[style=sparql]{code/bag_federation_service_q.rq}
  \end{minipage}

  \caption{A projection-free conjunctive query retrieving each person's job, age, e-mail, and identifier from a federation of three members.
  The job registry and the social network redundantly store every person's age and job.
  Under \ac{ASSF} the query returns a bag of results, whereas with \texttt{SERVICE} clauses it returns the same four solutions as a set.}
  \label{fig:bag-federation-example}
\end{figure*}

\Cref{fig:bag-federation-example} illustrates this.
A federation of three members, an identifier registry, a job registry, and a social network, redundantly stores part of its data: each person's age and current job are recorded by both the job registry and the social network.
The federated query retrieves each person's job, age, e-mail, and identifier.
It is a \ac{CQ} \emph{without projection}, so on a single endpoint it would return a set.
Yet, given the definition of the canonical \ac{ASSF}, \ac{ESA}~\cite{Cheng2021}, evaluating it over the federation duplicates solutions, the mapping for \texttt{ex:jan} is returned 4 times, so the query exhibits bag semantics, even though every federation member contains a set-semantic \ac{KG}.
The same query expressed with \texttt{SERVICE} clauses returns the same answer as a set.
\footnote{An executable demonstration of the semantics of federated queries, evaluated with the Comunica~\cite{taelman_iswc_resources_comunica_2018} query engine, is available at \url{https://github.com/constraintAutomaton/demo-federated-query-semantic}}

To the best of our knowledge, bag-set semantic SPARQL query containment remains unexplored, as does query containment in the context of federated queries.
\footnote{Note that QCan~\cite{salas2018canonicalisation} deals with bag-set semantics, however, it addresses query equivalence rather than query containment, by transforming SPARQL queries into a canonical form and performing syntactic comparison.}

This paper bridges these gaps by formalizing query containment for conjunctive SPARQL federated queries under bag-set semantics.
We first present the \hyperref[sec:related_works]{related works},
then the \hyperref[sec:preliminaries]{preliminaries},
the \hyperref[sec:semantic_federated_q]{semantics of federated queries},
followed by the evaluation of \hyperref[sec:federated_queries_query_containment]{federated queries query containment}
and we \hyperref[sec:conclusion]{conclude} the paper.
